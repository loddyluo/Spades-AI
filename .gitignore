__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.eggs/
.venv/
.claw/
venv/
data/*.pt
logs/
logs_old/
*.png
runs/
rl_checkpoints/
rl_checkpoints0531*/
logs_mid/
*.zip
.ipynb_ch*
*.md

# Node.js / frontend (gui) — never commit installed deps; run `npm install`.
# Note: the generic `dist/` rule above also strips package internals like
# gui/node_modules/vite/dist, so committing node_modules yields a BROKEN tree.
node_modules/
gui/node_modules/
SCL/
config/

# Locally compiled untagged solver binaries (arch-specific copies are tracked).
trick_taking/solvers/_*_core.so