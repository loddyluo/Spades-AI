# Spades AI 远程对战 — 使用方法

## 概述

两个人类玩家各在一台电脑上，通过浏览器连接服务器，搭档对战两个 AI（`RuleExactFirst4NilPlayer`）。每人只能看到自己的手牌，互不知道对方手牌。

```
┌─────────────────┐
│  game_server.py │  服务器（任意一台能联网的电脑）
│  ws://IP:8765   │
└───┬─────────┬───┘
    │         │
    │  浏览器  │  浏览器
    │  玩家 A  │  玩家 B
    │  座位 0  │  座位 2
    └─────────┘
```

## 启动服务器

在任意一台电脑上（需要有 Python 环境）：

```bash
cd Spades-AI
python gui/game_server.py --port 8765 --num-workers 10
```

输出示例：
```
Spades game server starting on ws://0.0.0.0:8765
  Mode: 2 humans (partners) vs 2 AI (RuleExactFirst4NilPlayer)
  Humans: connect with matching room code + seed + partner seats

Listening on ws://0.0.0.0:8765
```

### 服务器参数

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--host` | `0.0.0.0` | 监听地址 |
| `--port` | `8765` | WebSocket 端口 |

### 注意事项

- 服务器需要能被两台客户端电脑访问到。如果在同一局域网，直接用局域网 IP。如果跨网络，需要公网 IP 或内网穿透（如 frp、ngrok）。
- 防火墙需要放行对应端口（如 `8765`）。

## 启动前端

每台客户端电脑上都需要启动前端（或访问服务器上的前端）：

### 方式一：各自本地启动（推荐开发时）

```bash
cd gui
npm install    # 首次使用
npm run dev
```

浏览器打开 `http://localhost:5173`。

### 方式二：服务器构建后提供静态文件

在服务器上：

```bash
cd gui
npm run build
# 然后用任意 HTTP 服务器 serve dist/ 目录
# 例如：python -m http.server 8080 -d dist/
```

客户端浏览器访问 `http://服务器IP:8080`。

## 开始对局

1. 打开浏览器，在模式选择界面点击 **🌐 远程对战**

2. 填写连接信息：

   | 字段 | 说明 | 示例 |
   |---|---|---|
   | 服务器 | 游戏服务器的 IP:端口 | `192.168.1.5:8765` |
   | 房间号 | 两人必须相同，任意字符串 | `ABCD` |
   | 种子 | 两人必须相同，非负整数，决定发牌 | `12345` |
   | 座位 | 自己的座位，两人必须互为对家 | 玩家 A 选 0，玩家 B 选 2 |

3. 点击 **连接**

4. 两人都连接后，对局自动开始。

## 座位规则

| 玩家 A 座位 | 玩家 B 座位（搭档） | 队伍 |
|---|---|---|
| 0 (North) | 2 (South) | NS |
| 1 (East) | 3 (West) | EW |
| 2 (South) | 0 (North) | NS |
| 3 (West) | 1 (East) | EW |

如果座位选择不对（不是对家），服务器会提示正确的搭档座位。

## 对局流程

1. **等待对手** — 显示"等待对手加入…"
2. **叫牌** — 轮到叫牌时，底部出现叫牌按钮（Nil + 1~13）
3. **出牌** — 轮到你出牌时，合法牌会有金色边框，点击出牌
4. **等待** — 搭档或 AI 操作时显示状态文字
5. **结算** — 13 墩结束后显示得分

## 牌局中的信息

- **你的手牌**：显示在屏幕底部
- **搭档手牌**：**不显示**（只能看到牌背数量）
- **AI 手牌**：只显示牌背数量
- **桌面**：已打出的牌公开可见
- **已完成墩**：所有玩家可见

## 顶部栏说明

| 信息 | 说明 |
|---|---|
| 房间 | 当前房间号 |
| 种子 | 本局种子 |
| 你的座位 | 你的座位号和方位 |
| 搭档 | 搭档座位号（或"等待加入…"） |
| 断开 | 红色按钮，随时可断开返回菜单 |

## 常见问题

### 连不上服务器

- 确认服务器已启动
- 检查 IP 和端口是否正确
- 检查防火墙是否放行对应端口
- 如果跨网络，确认公网可达

### 种子不匹配

两人输入的种子必须完全相同。种子决定了发牌，不同种子会发不同牌。

### 房间号被占用

换个房间号即可。房间号是任意字符串（会自动转为大写）。

### 对局中卡住

可能是 AI 精确求解器计算时间较长（后 36 张牌每步可能需要 1-4 秒），耐心等待即可。如果长时间无响应，点击"断开"重新开始。

### 只有一台电脑怎么测试

可以开两个浏览器窗口（或一个正常窗口 + 一个无痕窗口），分别连接 `localhost:8765`，输入相同房间号和种子，选择对家座位，即可本地测试。
