# ---------------------------------------------------------------------------
# Spades 对战「公网部署」专用依赖（运行时最小集）。
#
# 只列出 gui/backend.py 与 gui/game_server.py 运行链路用到的库：
#   - torch  : 加载 residual-Q acting bidder 与 bid_nsfp belief bidder
#   - numpy  : determinization / 重要性采样
#   - pyyaml : HyperparamConfig.from_yaml 读 configs/8.yaml
#   - tqdm   : strategy/__init__.py 会连带导入 truncated_mcts_strategy，
#              后者顶层 `from tqdm import tqdm`，故为硬依赖（体积极小）。
#   - websockets: 双人联网模式 gui/game_server.py 的传输层
#
# 训练/评估用的 transformers、tensorboard 不在对战链路，故不安装，
# 可把镜像缩小约 1GB+。
#
# 用法：  pip install -r requirements-serve.txt
# ---------------------------------------------------------------------------

# 只拉 CPU 版 torch（默认源会拉带 CUDA 的 2GB+ 包）。
--extra-index-url https://download.pytorch.org/whl/cpu

torch
numpy
pyyaml
tqdm
websockets
