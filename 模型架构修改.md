现在您要应对的训练的文件是/root/Spades-AI/rl/pretrain_rl_multicpu.py（只训练1个模型）

为了简化模型所做的决策，现在每一张牌如果是跟牌，让模型做下面的3项选择题：
1、出手里的最小牌（如果是跟牌，最小牌指的是优先垫最短的花色中的最小张）
2、出手里的比目前桌子上的所有牌大的最小牌（例子：如果是第一个将吃的人，那么比目前桌子上的所有牌大的最小牌就是黑桃最小牌。如果不是第一个将吃的人，那么要出比目前最大黑桃大的最小牌）
3、出手里的最大牌（如果是将吃，那么就是出黑桃最大牌）
（注意，有些时候不一定每个选项都可以选）
如果是自己是一墩第一个出牌，让模型还是做52项选择题，选择一张牌（52项选择题）

也就是说，模型的输出维度是55维的，如果是自己是一墩第一个出牌，使用前52个维度，不然，使用最后3个维度。（需要有一段代码，解释模型的3项选择分别对应出什么牌，以及这3个选项是否存在）


不要管async.py那个文件，也不要管/root/Spades-AI/rl/train_rl_multicpu.py那个文件。

下面的指令必须能运行
```bash
python rl/pretrain_rl_multicpu.py --num-games 20000 --seed 42 --lr 0.0005  --num-workers 32 --update-interval 128 --num-epochs 1 --entropy-coef 0.25 --save-interval 128

python rl/eval_rl_multicpu.py --num-games 5000 --seed 6020000 --num-workers 30 --load-checkpoint /root/Spades-AI/rl_checkpoints/pretrain_170dim/pretrain_policy_final.pt
```